doctype 
html
  head
    title= title 
    link(rel='stylesheet', href='/stylesheets/bootstrap.min.css')

  body
    .container

nav.navbar.navbar-default( role="navigation" )
    .navbar-header
        button.navbar-toggle.collapsed( type="button", data-toggle="collapse", data-target="#navbar-inverse", aria-expanded="false", aria-controls="navbar")
            span.sr-only Toggle navigation
            span.icon-bar
            span.icon-bar
            span.icon-bar
        a.navbar-brand(href="/") F5 Guru Demo
 
    #navbar-inverse.collapse.navbar-collapse
        ul.nav.navbar-nav.navbar-right
            if (typeof(user) !== 'undefined')
              li.text-capitalize: a( href="#") Welcome #{user}
            else
              li: a( href="#") Login
            li: a( href="/" ) Home
            li: a( href="#" ) Help

if (typeof(user) !== 'undefined')
  block content
else 
  .container
    .page-header
      .row
        .col-lg-8.col-md-7.col-sm-6
          h1.text-danger Error
          div.alert.alert-dismissible.alert-danger
            button.close &times;
            strong Oh snap!
            p You have to login to use this application

footer
  div.row
    div.col-lg-12
      p.small.center Theme by #[a(href="https://bootswatch.com/superhero/") Bootswatch]